# 複製此檔為 .env（與 app.py 同層），再填入金鑰。勿將 .env 上傳到 Git。
#
# ── Gemini（AI 速覽優先）：到 Google AI Studio 建立 API 金鑰
#    https://aistudio.google.com/apikey
#    文件：https://ai.google.dev/gemini-api/docs/quickstart?hl=zh-tw
GEMINI_API_KEY=
# 選填；預設在 config.py 為 gemini-2.5-flash（1.5 已停用）。其他範例：gemini-3-flash-preview
# GEMINI_MODEL=gemini-2.5-flash
#
# ── 未設 GEMINI 時可改用 OpenAI 相容 API（base_url 可填相容服務）
# OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
#
# ── 疑難排解（與網路上「Gemini 1.5 + google-generativeai」舊文可能不符）
# A. 本專案依賴的是新版套件 google-genai（不是舊的 google-generativeai）。請：
#      pip install -U google-genai
# B. gemini-1.5-flash / gemini-1.5-pro 已退役，請改 GEMINI_MODEL（見 config.py 預設或官方模型列表）：
#      https://ai.google.dev/gemini-api/docs/models
# C. 金鑰須為 Google AI Studio 的 API key；本程式未串 Vertex AI。若你用 Vertex，
#    需在別專案用 vertexai=True + 專案/區域設定，模型名稱格式也不同，與此 .env 路徑不同。
